<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pentatrespassers.neodoollae">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nedoollae">
        <activity
            android:name=".view.login.main.friend.FriendProfileActivity"
            android:exported="true" />
        <activity
            android:name=".view.login.main.home.RoomProfileActivity"
            android:exported="true" />
        <activity
            android:name=".view.login.main.home.AddRoomActivity"
            android:exported="true"
            android:noHistory="true" />
        <activity
            android:name=".view.login.MainActivity"
            android:exported="true" />
        <activity
            android:name=".view.LoginActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="7qdjutvncl" />
    </application>

</manifest>