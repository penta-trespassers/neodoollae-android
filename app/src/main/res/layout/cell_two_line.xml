<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:id="@+id/twoLineConstraint"
    android:background="@drawable/bg_common_button_menu_cell">

    <ImageView
        android:id="@+id/twoLineImage"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:srcCompat="@drawable/ic_people_black_24dp"
        app:tint="@color/trespassGray_600" />

    <TextView
        android:id="@+id/twoLineMainText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:textColor="@color/black"
        android:textSize="14dp"
        android:textStyle="normal"
        app:layout_constraintStart_toEndOf="@+id/twoLineImage"
        app:layout_constraintTop_toTopOf="@id/twoLineImage"
        tools:text="MainText" />

    <TextView
        android:id="@+id/twoLineSubText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="@color/trespassGray_600"
        android:textSize="10dp"
        android:textStyle="normal"
        app:layout_constraintStart_toStartOf="@id/twoLineMainText"
        app:layout_constraintTop_toBottomOf="@id/twoLineMainText"
        tools:text="SubText" />

</androidx.constraintlayout.widget.ConstraintLayout>